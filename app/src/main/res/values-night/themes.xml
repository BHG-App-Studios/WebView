<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Kept aligned with values/themes.xml: Theme.Material3.DayNight.NoActionBar
        already resolves the night palette, so no per-night overrides are needed.
    -->
    <style name="Theme.WebsiteAppBuilder" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
